# 需求台账（Backlog）

> **这是「owner 提过的要求」与「提出的问题」的唯一台账。**
> 与 `roadmap.md`（任务分解）、`threads/`（谁在做）的分工见文末。
> 最后更新：2026-09-17 16:10（#101 话题物件可见性已上线并独立复核；补 #99 的事后复核 —— 三处「代码在撒谎」）

## 为什么需要它

2026-09-16 owner 问「我给过开发线程的问题，有没有存到一个特定区域？」——实查结论：**没有**。
owner 的反馈此前散落在三处，每处都不适合当台账：

| 曾记在哪 | 为什么不合适 |
|---|---|
| `docs/threads/audit.md` 的一节 | 按设计「超 24 小时可删」，且开发线程按约定只看自己的文件 |
| `roadmap.md` / `acceptance.md` | 它们是**代码完成度**，不是「owner 要什么」；且已停在 09-14 |
| 本地记忆 / 对话 | 换机器就断，开发线程看不到 |

后果是真实的：owner 在 09-15 提的「广场观感不够顶级 / 能动么」，
**中间有一段时间处于「提过但没人接手」的状态**，直到 #77 才被认领。

## 用法（三条硬规矩）

1. **owner 提要求 → 先落这里再开工。** 不许只写在 PR 描述或对话里。
2. **PR 合并时回来改状态。** 状态只有四档：`待认领` / `在做` / `已上线` / `不做（附理由）`。
3. **每条必须能追溯到证据** —— owner 的原话、PR 号、或线上实测，不写「据说」。

---

## A. 待认领（有人提过，还没人做）

| # | 要求（owner 原话或原问题） | 来源 | 状态 | 证据 / 备注 |
|---|---|---|---|---|
| A1 | **「现在的视觉效果太差了 看起来不牛逼」** —— 并要求出一份问题审计与重构方案 | owner，09-16 16:30 | `在做`（owner 判定**不到位**；P3 已授权，但前提被审计推翻，见下） | 已量化：舞台 1,190,880 px²，主体+影子仅占 **4.03%**（人 1.49% / 影 2.54%），背景 95.97%；「有主体」的画面通常 15–40%。**#77 已上线**（亮场地 + 全场影子 + 缺口成地灯，16:58 部署），光影守卫已由 #85 接进 CI（`check:light`，实测在 CI 里跑了 7 节断言）。`docs/square-visual-audit.md`（273 行）是对这条的量化审计。**owner 已判定：不到位**（09-16 18:44）。
**审计线程像素级取证**（线上真实截图 1440×900，舞台=左75%）：平均亮度 14.7→**45.3**（确实变亮），但**亮面/光池仅 1.52%→2.58%**、**暖色（地灯）仅 0.65%** —— 即「把均匀的黑换成了均匀的灰蓝」，**没建立起可辨认的场地结构**。根因（作者自述、我已核代码）：节点 `SIZE_MAX=320/SIZE_MIN=132` 硬编码、而真实 `heat` 全部≈1 → 21 张卡全塌到 197px，人群外接半径只有 71px → **每节点内部 42% 是空的**。
**⚠️ 我实测了作者留的 P1（scale 0.92→1.35）：亮面只 1.25 倍，而视野面积只剩 46%** —— 等比缩放不改变同一内容占比，故 P1 不是出路（与作者「不建议继续调色」的判断一致）。
**✅ #101 已上线（2026-09-17 16:0x，`dcedfac1`）—— 修的是「话题物件看不见」，A1 的一部分，不是全部。** 审计线程独立复核：① **对比度我自己实现了一遍 WCAG**（从 CSS 解析颜色、按 alpha 合成）：`rgba(255,214,178,0.22)` → **1.70:1**，`#ffe6c8` → **16.40:1**，与 CI 日志里守卫打印的 16.40 一致；② **负向验证我自己做了 3 处注入**（改回旧 fill / 对调 `part="aura"`·`part="body"` 顺序 / 把本体 size 改成不一致），三条守卫**全部点名拦下**，还原后回绿 —— 守卫不是恒绿的摆设；③ **线上 CSS 实测已生效**（`.sq-relic-body{fill:#ffe6c8;…}`、`.sq-relic-detail` 已存在、svg 74%、场地降淡），冒烟 8/8。**⚠️ 但 A1 的根因未动**：节点 `SIZE_MAX=320`/`SIZE_MIN=132` 硬编码、真实 `heat` 全≈1 → 21 张卡全塌到 197px、**每节点内部 42% 是空的** —— #101 只让那件物件在人群里「亮起来、不被挡」，**没有改变「主体只占 4.03%」这个总量问题**。改尺寸会动「卡片大小=热度」的产品语义 → **要 owner 定**。另：#101 的 PR 标题写「1.72:1 → 16.40:1」，**1.72 是笔误**（守卫真实算出的是 1.70；该数字还硬编码在守卫的失败提示文案里，建议改成动态打印实际值）。 |
**⚠️ 事后复核 #99（广场第二轮，审计线程 2026-09-17 16:0x）** —— 该 PR 在我离线期间自行合并，改动本身成立（气泡全用答主原话、双保险反查署名、每簇自发光、fold 姿态 29% 头身间隙已修），但发现**三处「代码在撒谎」**（不影响用户可见功能，属技术债）：① `SquareCanvas.tsx:371-377` 的注释描述的是**该 PR 自己废掉的设计**（「全广场唯一的那一盏灯」+ 引用已删除的 `lightSourceOf` + 「光移过去影子同时转向」），而它注释的 `hottest` 实际只服务看山注视；② `kanshanGazeAt`（`dialogue.ts:138`）**产品代码 0 调用点**（只有守卫在用），而 SquareCanvas 走的是另一套 `kanshanTarget` → **「看山看谁」两套逻辑并存**，恰是该 PR 自己在 `answersOf` 处刚修过的那类平行实现；③ `RELIC_RADIUS_RATIO=0.3` 与真实渲染的 `d*0.34`（即 `radius*0.34`）不一致、且产品不用它 → 用它的 `audit-square-visual.mjs`（已接进 `check:logic`）报的物件面积**系统性偏小约 28%**。**我同时撤回了一处自己的误报**（`speechAt`/`speakingClusters` 被判「无调用点」，实为 `bubblesAt` 同文件内自用 —— 我的脚本只统计跨文件调用）。详见 PR #99 评论。 |
| A2 | **「小人有动效么，可以更顶级么」** —— #77 之前的原始表述是「小人挺一般的、平庸」「能动么」 | owner，09-15 晚 | `部分上线` | 09-15 那轮只做到「保留下一步」，**当时没有落进任何台账**（这正是建本台账的直接原因）。**#77 已上线静态光影**（场地/影子/体态/纵深）；**动效部分（人群呼吸 / 光柱脉动 / 指针交互）仍未做，待认领** |
| A3 | **「最后一轮网站收敛清单」** —— 本轮只做三件事：① 从提问到首轮分身回答 ② 虚拟广场和现场广播 ③ 真人补充入口；P1 只修现有页面的明显断点。清单同时规定：不重建蒸馏/生成服务/图谱后端，已通过的项目不重复开发 | owner，09-16 傍晚（正文后附清单全文） | `在做`（#92 已认领 P0-3，声明先行） | **该清单由提 #90 的线程用真机 CDP 逐条核验（37 条：29 已通过 / 3 需修改 / 5 尚未实现）**；审计线程已独立复核，结论一致（见行末）。 整块没做出来的是 **P0-3（真人补充与搬运）**，五项：①「我已在知乎发布」按钮仍在（`components/mirror/HandoffPanel.tsx:124`、`components/mesh/MineHandoffPanel.tsx:207`）；②「先让真人补充内容」的语义仍是「打开知乎编辑器」，没有「复制邀请文案」；③「邀请内容已复制，请自行发送给答主」「这是 AI 草稿，请由本人修改或补充」全仓 **0 命中**；④ 长接口解释（「我们审计了知乎开放平台全部 54 个接口文档…」）未换成「本站不能替你在知乎发布。复制并编辑内容后，请由本人自行发布。」；⑤「邀请补充」进的是 `/fill` 真人补充页，不是「同一份当前回答的搬运区域」。**P0-1 两处不符**：① 首页仍写「或粘贴知乎问题链接…」+「支持知乎链接」，但实测输入 `zhihu.com/question/…` **不会被解析成标题**（解析只存在于搬运/知乎接口层）；② 未选人时按钮**可点**，会静默降级为「让看山推荐并作答 →」。**P0-2 全过 9/9**（含「两块互不带动」「拖后不误触」两条最易出问题的）→ **不要重复开发**。完整报告与开发用点选填表：https://hb.cauai.fun/show/heikesong-converge-audit/ 。**审计线程独立复核（2026-09-16 17:55，`git grep` 全仓，不看报告）**：①「我已在知乎发布」确实仍在（`HandoffPanel.tsx:124`、`MineHandoffPanel.tsx:207` 等 11 处命中，含注释与文档）；③ 长接口解释确实仍在（`HandoffPanel.tsx:104`）；**但 ② 那两处新文案已从「0 命中」变为各 3 处命中** —— 逐条看**全部落在文档里**（清单原文 + #92 的线程声明），**不是实现**：`#92` 是「声明先行、开工中」，尚未动代码。→ 接手者请以**重新 grep 一次**为准，本结论有保质期。口径差异一处：本行原写「审计线程已核验」，实为提 #90 的线程所做，已更正。<br>**⚠️ 审计线程复核 #96（2026-09-17 01:10，结论：先不合）** —— 3.1/3.2/3.3/3.4 逐字核实**全部成立**；**3.5 实际未落实**：`inviteAnswerId` 无任何调用点传入（全仓唯一 `<HandoffPanel />` 没传）→ 永远走 fallback「固定第一位答主」，**正是清单要求排除的行为**；且 `invited.id`（`cand-<hash>`，来自 `gap.ts`）与 `copyInvite` 要匹配的 `answer.id`（`ans-<skillId>`，来自 `server/mirror.ts`）**不同 id 空间**，直接传也匹配不到。另两处：**用户可见文案漏了 markdown `**`** —— `mirror/page.tsx:245` 会显示在页面上、`handoff.ts:156` **在剪贴板正文里**（全仓无 markdown 渲染器 → 星号原样发出，且违反 `lib/server/mirror.ts:502` 自己定的「不要写 Markdown 标记」）；线程声明把「`confirmed` 失去生产者」写成了「一并清理」（实测 `confirmHandoffFor` 定义/导出仍在、`types.ts:262` 仍含该值、`MyMeshPanel.tsx:74` 仍读它 → 恒假分支 + 无调用者导出）。**根因**：守卫 §⑫ 的断言打在「提到过 `copyInvite`」而不是「真的在用它」→ 天生拦不住「传进去的是 undefined」。审计意见全文见 PR #96 评论。<br>**✅ 已闭环（2026-09-17 11:15）** —— **P0-3**：审计线程接手修正后 **#96 已合并**（`9a352656`）、**#97** 声明同步更正（`204a430c`）。3.1–3.4 **保持作者原实现**（逐字核实成立）；**3.5 收窄为「入口指向搬运区域」**（有效），「按回答定位」**未实现** —— 原实现是个没人传值的死参数，已删（口径问题另立 **A6**）。**P0-1**：**#98 已合并**（`365b0f36`）—— ① 去掉输入框的链接解析承诺（走清单原文允许的「改提示」路线，未新增解析能力）② 未选人时按钮禁用 + `run()` 内再挡一层。新增守卫 `check:home`（9 条断言）并接进 `check:logic`，**已在 CI job 日志中确认真的跑了**（不是只写进脚本）。线上复验：冒烟 8/8、来源三处同数 190/190/190、`main = 线上 = 365b0f36`。 |


| A4 | **owner 授权「扩到每簇 6–8 位分身」（约 130 次直答）** —— 目的是让人群变密 | owner，09-16 19:07（选 A） | `前提被推翻，待 owner 重定` | **审计在花钱前实测了可行性，结论：按原方案拿不到「6–8 位真人」。** ① `recommendPersonas(title, count)` 的 `count` 默认 = `MIN_SKILLS`=3，`MAX_SKILLS`=6 本是预留上限，**改 count 是一行**；但实测 22 题：**只有 6 题**能因扩量增加真人（各 +3），**6 题一位对口真人都没有**，其余 10 题只能到 1–2 位；扩到 count=6 时**真人 33→51，而通用视角（亲历者/拆解者/反驳者，非真人）占 40%**。② 库内 66 篇回答只涉及 **10 位**答主，其中**半佛仙人 21 篇 / 张佳玮 13 / 贱贱 12 / 大猛 8 = 82%**，16 位名册有 6 位从未出现。③ 故**真瓶颈是名册覆盖不足（16 位答主覆盖不了 22 个话题），不是生成次数**。**建议改法见下**（0 额度优先） |

> ### A4 的执行建议（审计给出，按性价比排序）
>
> | 路径 | 做法 | 额度 | 我的判断 |
> |---|---|---|---|
> | **① 换题（推荐先做）** | 22 题里 **6 题 0 位对口真人**（近视配镜 / 借钱给亲戚 / 学区房 / 国际学校 / 长期加班 / 父母同住）→ 换成名册有对口的题 | **0** | 这些题现在的答主是硬凑的，换了立刻「每场都是对口的人」 |
> | **② 只给能到 6 位的题扩量** | 只对「c=6 真人」的 6 题扩（大厂转行 / 咖啡店 / 考研 / 月薪两万买房 / 35 岁被优化 / 存款 50 万） | 约 **18** 次（非 130） | 真增益，且不引入通用视角 |
> | **③ 扩名册** | 16 → 30+ 位答主，才可能让每场都有 6–8 位**对口**真人 | 远超 130（每位新答主需爬取+蒸馏） | **真正的解**，但超本次授权范围 |
> | ④ 全量扩到 count=6 | 22 场全部 6 位 | 约 132 | ⚠️ **40% 会是通用视角**，且 6 题仍无真人 → **不推荐** |

---

| A5 | **「这场生成出来的关系，功能一直没太做好，可以先删」** —— 即 `/mirror` 上的「本场关系图」（`buildMesh`） | owner，09-17 00:10 | `已上线` | **删的不只是一个区块，是三处「会撒谎的残留」**（详见 D. 之外本行备注）。① `/mirror` 的 `id="mesh"` 区块 + `MeshGraph` 渲染 + `buildMesh()` 定义全部删除；② `/fill` 提交成功页那句「会多出节点和边」的承诺**必须一并撤掉** —— 删图之后**没有任何一张图**会因补一段真人而变化（`/me` 的 `buildCorpusMesh` 只看关键词共现，实测补一条真人后节点/边数一个都没动），所以**只能撤承诺，不能把链接换成 `/me?tab=mesh`**；③ 看山流程第 8 步 `更新 Mesh · 关系图长出新的边` → `补充已写入 · 这一段并进最终稿，闭环完成`。**保留**：`/me` 的「我的 Mesh」（`buildCorpusMesh`，讲的是「我创作过什么」，与本场无关，也是演示视频第 9 段要用到的那张图）、`/mesh` 重定向路由、`MineHandoffPanel` 搬运面板（它在「我的 Mesh」里，不在这场里）。**守卫**：`scripts/check-square-crowd.mjs` ⑥ —— 注入 4 处（buildMesh 定义 / `id="mesh"` / `MeshGraph` / `/mirror#mesh` 死链）实测全被点名，还原后回绿；`scripts/mesh-selftest.mjs` 改为只验 `buildCorpusMesh`（8 项断言全过） |

| A6 | **「邀请补充」到底要定位到哪一位？** —— 清单 3.5 要求「不能固定第一位答主」，但**没定义「按谁定位」** | 审计线程，09-17（复核 #96 时发现） | `待 owner 定` | 现状：`copyInvite()` 取的是「第一位还没被真人补过的**分身**」，而用户点的是缺口里的**真人候选** —— 两者不是同一类对象，id 也不同源（候选 `cand-<hash>` 来自 `gap.ts`，回答 `ans-<skillId>` 来自 `server/mirror.ts`）。**所以「把参数接上」解决不了问题**，得先定口径：① 邀请**真人作者**（那 `toInviteText` 要改用候选名，现在的文案写的是分身名）② 邀请**分身答主**（那「缺口候选」就不该出现在邀请流程里）。**定下来之前不写代码** —— 详见 `docs/threads/dev-converge-p03.md` 的「审计修正」一节 |
| A7 | **对外材料仍在承诺「粘贴知乎问题链接」** —— `docs/prd.md:35/79`、`docs/product-plan.md:50/142`、`docs/submission.md:44/135` | 审计线程，09-17（复核 #98 时发现） | `待 owner 定` | #98 已把**输入框**那句承诺删掉，但文档还写着「用户输入真实问题（或粘贴知乎问题链接 / 从热榜挑）」，以及「`GET /api/v1/content/question_answers`：用户粘贴知乎问题链接时，读取该问题下的真实回答摘要」。实测：全仓**没有任何客户端代码调用** `/api/zhihu/question-answers`（只有 `docs/architecture.md:159` 提到该路由存在）→ 那句描述了一条**走不通的路径**。⚠️ 但**不是「完全没做」**：`lib/server/mirror.ts:227/232` 确实会识别链接并标 `origin: "zhihu-link"`、把 `sourceUrl` 存下来供搬运深链用 —— 只是**不解析标题、也不取该问题的回答**。**submission.md 是对外提交物，改它要 owner 点头**（也可能反过来：决定真去做解析） |

## B. 已上线（owner 提过，已完成）

| # | 要求 | 来源 | 状态 | 落到哪 |
|---|---|---|---|---|
| B1 | 右栏被切掉 / 滚不动；「查看详情」「互相回应」两处报错；名册浮层被后面卡片盖住 | owner 实机截图，09-15 晚 | `已上线` | #54 · 线上复验 43/43 |
| B2 | 互相回应时邀请 >8 位 / 长回答 >4000 字必 400，且报错是英文直接弹给用户 | owner，09-15 晚（#54 残留） | `已上线` | #58 · 实测 12 条/6000 字 → 200；25 条 → 中文 400 |
| B3 | 「信息架构收敛 + 文风去 AI 味 + 答主扩容」（一次 10 条） | owner，09-15 上午 | `已上线` | #9 / #19 / #14 / #35 / #52 等 |
| B4 | 卡片上「未验证 / 未抓取全量回答」这类免责式标签要去掉，**且不做中性替换** | owner 明确纠正：「第五点不要改中性表述 我的意思是删了就别加了 懂么」 | `已上线` | #7 · 守卫 `check:square-crowd.mjs` |
| B5 | 广场要「人群广场」而非矩形话题卡；缺口用橙色区分 | owner 概念图 + 产品说明，09-15 | `已上线` | #41 · #51（orange 只属于缺口） |
| B6 | 首页不要流入广场内容（与 `/square` 重复）；看山不要重影 | owner，09-15 | `已上线` | #19（删 FeedStream）· #33（消除看山重影） |
| B7 | 「带他去提问」这类入口要删干净 | owner，09-15 | `已上线` | #19 |
| B8 | 登录「点了没反应」/ 登录了却用不了 | owner，09-15 | `已上线` | #16（回调丢参）· #23（token 跨重启存活） |
| B9 | 十六项迭代清单（逐条核对是否已解决并上线） | owner，09-15 | `已上线` | 当时结论 8 已解决 / 5 部分 / 3 未解决；后续 #14 / #17 / #19 补齐 |

## C. 已裁决不做（附理由，免得被当 bug 重修一遍）

| # | 事项 | 裁决 | 理由 |
|---|---|---|---|
| C1 | 直答额度口径「不一致」（`AGENTS.md` 写 100/天，实测平台 5000/天） | `不做` | **不是缺陷**：100 是**自我约束的支出上限**，不是对平台配额的陈述；自限更严方向是对的。若嫌歧义，正确改法是加半句说明，而不是把 100 改成 5000（那是把节流阀拆了） |
| C2 | 公共人物（6 位）何时真正可调用 | `不做（本轮）` | `isCallable()` 要求 ≥2 条带 `verifiedAt`/`locator` 的 https 来源。**刻意保持不可用**：一位人物再有名，也不因此变成可调用的能力。#69 已把页面文案从「思维分身」改成「能力草案（核验中）· 已核验 0/24」 |
| C3 | `/mesh` 与 `/discover`、`/feed` 路由 | `不做` | `/mesh` 已是 meta-refresh 到 `/me?tab=mesh`；另两个已失效且零引用、已出 sitemap，属设计内 |

## D. 审计线程自己提出、仍未闭环（不是 owner 要求，但需有人认领）

| # | 事项 | 状态 | 证据 |
|---|---|---|---|
| D1 | 部署打断在途会话：旧 HTML 引用的 chunk 下次构建后 **404 + `content-type: text/html`** → `ChunkLoadError` | `已上线` | **#81 已修**（build 到 `.next.new` 后原子 rename 切换，+`check-atomic-deploy.mjs` 21 条断言并独立接入 CI）；线上实测 `deploy-atomic.sh` 已就位。⚠️ **顺带更正审计线程自己的记录**：此前记成「500」，#81 实测为 404+text/html，我独立复核确认 **#81 是对的** |
| D2 | 自动巡检 automation（每小时盘点队列 + 线上复验）当前为 **PAUSED** | `待 owner 定` | 本轮全流程是手动跑的；恢复或改周期需 owner 发话 |
| D3 | 本仓库无 **Issue 模板**（`.github/ISSUE_TEMPLATE/`），提问题时字段全靠自觉 | `待认领` | 全仓只有 1 个 issue（#70，已关闭）——说明 issue 事实上没被用起来 |
| D4 | 未知 / 旧 handle 的档案页当前是「HTTP 200 + `noindex`」（#72 的实现），**可能更应直接 404** —— 需 owner 定口径 | `待 owner 定` | 这是审计线程按「别让搜索引擎收录空页、但别对活人甩 404」判的，**属建议项，不是 owner 的原话**。若 owner 要 404，改法很小（`notFound()` 替掉提示页） |

---

## 与其它文档的分工（互补，不重复）

| 文档 | 回答的问题 | 时点 |
|---|---|---|
| **`backlog.md`（本文件）** | **owner 要什么、做完没有** | 长期 |
| `roadmap.md` | 代码层面还剩哪些任务、优先级 | 里程碑 |
| `acceptance.md` | 验收项逐条是否通过 | 交付前 |
| `threads/` | **谁**在做、正在改哪些文件 | 开工时（短效，24h） |
| `prd.md` / `architecture.md` | 产品与技术事实 | 长期 |

## 维护

- **谁改**：任何线程都可以改（改状态时在 PR 里说明依据）。
- **什么时候改**：① owner 提新要求时；② 一个 PR 合并后回头改它对应的行。
- **不许做**：把「待认领」悄悄删掉 —— 要么改成 `已上线`（附 PR 号），要么改成 `不做`（附理由）。
